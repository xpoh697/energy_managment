# Energy Management System - Development Log (AI-Persist)

Данный файл предназначен для того, чтобы ИИ-ассистент мог быстро восстановить контекст проекта и не повторять ошибки прошлых сессий.

## 💾 Данные и диагностика
- **Backup базы:** `\\192.168.100.5\config\energy_management_backup.json`
- **Инструкция для ИИ:** 
    1. **ОБЯЗАТЕЛЬНО:** Перед внесением любых исправлений или дополнений в код — сначала полностью ознакомься с этим файлом (`DEVELOPMENT_LOG.md`).
    2. Если тебе не хватает данных для понимания или дебага, **всегда проси пользователя обновить бэкап** и прислать путь к нему.
    3. При получении новых знаний об архитектуре, исправлении важных багов или изменении логики — **обязательно фиксируй их в этом файле**, чтобы контекст не терялся между сессиями.

## 🏗 ОПРЕДЕЛЕНИЯ РЕЖИМОВ ИНВЕРТОРА (ПО ПРИОРИТЕТУ АЛГОРИТМА):
1. **buy:** (Высший приоритет) Импорт из сети, батарея заряжается от сети, дом питается от солнца + сеть.
2. **bat_emergency:** (Аварийный SOC, нет солнца) Нет разряда батареи. Допускается дозаряд от солнца до уровня Min SOC + 1%. Дом питается от сети/солнца.
3. **stop_sale:** (Аварийный SOC + Солнце ИЛИ Ожидание заблокированной продажи) Нет экспорта в сеть, батарея ЗАРЯЖАЕТСЯ от солнца, дом питается от солнца/батареи.
4. **no_pv_sale_no_bat:** (SOC в норме + Ожидание ямы) Нет экспорта в сеть, НЕТ заряда батареи, дом питается от солнца в первую очередь.
5. **sale_pv:** (Режим по умолчанию / Добор при хорошей цене) Порядок распределения солнца: ДОМ, БАТАРЕЯ, СЕТЬ.
6. **sale_pv_no_bat:** (Продажа только солнца) Нет заряда батареи, дом питается от солнца, излишки уходят в сеть.
7. **sale_pv_bat:** (Активная продажа) Продажа и солнца, и накопленного в батарее.

## 🏗 Архитектура и ключевая логика

### 1. Симуляция заряда (StrategyEngine.run_soc_simulation)
- **Функция:** Просчитывает SOC на 24-48 часов вперед.
- **Возвращаемое значение:** `(soc_dict, log_list, overflow_kwh)`. 
- **⚠️ ВАЖНО:** Всегда ожидает распаковку **трех** значений. `overflow_kwh` — это потенциальный экспорт (излишки AC после 100% заряда).
- **v11.1.15 - Blended Anchor:** Первый шаг симуляции плавно смешивает живую мощность и профиль (вес = `minute/60`). Это убирает «пилу» (sawtooth) на стыке часов.

### 2. Расчет базового потребления (Base Consumption)
- **Логика:** `Base = Total_Meter - Managed_Loads`.
- **Managed Loads:** Нагрузки, имеющие свои датчики энергии (бойлер, посудомойка и т.д.).
- **v11.1.12 - Synthetic Floor:** Если расчетное значение `Total - Managed` падает ниже 70% от исторического среднего, система использует значение из профиля как «пол».
- **Сброс датчиков (v11.1.3):** Если дельта за один замер > 2.0 кВт⋅ч, она игнорируется (защита от скачков).

### 3. Сенсор отклонения (ConsumptionDeviationSensor)
- Сравнивает текущую нагрузку (`actual_base`) с историческим профилем.
- Чтобы избежать "-100% отклонения", формула `actual_base` включает текущий почасовой аккумулятор.

## 🚀 Важные правки и версии

- **v11.1.8:** Исправлен провал базового потребления и двойной учет в отклонении.
- **v11.1.9:** Потенциальный экспорт переведен на базу симуляции.
- **v11.1.11:** Асимметричное обучение BMS.
- **v11.1.20:** Исправлено отображение атрибутов арбитража.
- **v11.1.21:** Логика "лечения" (Healing) истории генерации.
- **v11.1.22:** Изменена стратегия при отрицательных ценах (Greedy Allocation / Force Buy).
- **v11.1.23:** "Лечение" профиля потребления (Consumption Healing).
- **v11.1.24:** Исправлен расчет "Кошелька" (Saldo) и ROI.
- **v11.1.26:** Баг кошелька при отсутствии индивидуальных датчиков.
- **v11.1.30:** Безусловная зарядка при отрицательных ценах.
- **v11.1.32:** Поддержка сенсора напряжения АКБ и `charge_amps`.
- **v11.1.39:** Добавлен режим `no_pv_sale_no_bat` (Ожидание отрицательной цены).
- **v11.1.40:** Удален режим `idle`. Режим по умолчанию — `sale_pv`.
- **v11.1.41:** Исправлено отображение ожидания в прогнозе.
- **v11.1.44:** Радикальное изменение приоритетов: режим ожидания теперь имеет наивысший приоритет.
- **v11.1.46:** Удаление `sim_log` из `_get_mode_at` (ликвидация NameError).
- **v11.1.48:** Приоритет ожидания над аварийным min_soc.
- **v11.1.49:** Унифицирована логика времени (0-47), добавлена диагностика условий.
- **v11.1.50:** Иерархия Buy > Emergency > Waiting.
- **v11.1.51:** Попытка добора в ожидании (использован неверный режим).
- **v11.1.52:** Тотальный рефакторинг режимов. Для добора солнца без экспорта используется **stop_sale**.
- **v11.1.53:** Уточнение добора: Если цена хорошая — **sale_pv**. Если цена плохая («яма») — **stop_sale**.
