## [2026-05-09 09:54] Задача: Расширенная отладка BUY и исправление видимости атрибутов

### Archi
1. Перемещаю `buy_debug` в конец блока атрибутов в `sensor.py`, чтобы исключить проблемы с отображением в HA.
2. В `strategy_buy.py` добавляю в отладку `candidates` и `target_hours`. Это покажет, какие часы алгоритм счел подходящими.

### Skeptic
1. **Logic Anomaly**: Если цена 0.74 активна при наличии 0.30, значит `min_p` в алгоритме не равен 0.30. Нужно проверить, не фильтруются ли цены на 13:00 до расчета `min_p`.
2. **UI Integrity**: Убедись, что `buy_debug` возвращается как словарь, а не как строка "Нет данных".

### Заключение
Решено:
- Обновить `sensor.py` (порядок атрибутов).
- Обновить `strategy_buy.py` (расширенная отладка + проверка лимитов).
