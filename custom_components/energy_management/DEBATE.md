## [2026-05-14 12:45] Задача: Ставлю ручной разряд до 59% а на карточке разряд до 55.7. Почему?

### Archi
В `strategy_base.py` в методе `run_soc_simulation` при обработке `hourly_manual_overrides` извлекается только `mode`, но игнорируется `soc_limit`. Из-за этого симуляция разряжает батарею на полную мощность `max_batt_p` до системного минимума, игнорируя установленный пользователем порог. Нужно внедрить `soc_limit` как динамический пол/потолок для конкретного часа симуляции.

### Skeptic
1. **Приоритет фаз**: В симуляции есть Фаза А (дом) и Фаза Б (продажа). Сейчас Фаза А всегда разряжает до `b_min_soc`. Для ручного режима нужно, чтобы лимит действовал на обе фазы, иначе нагрузка дома "просадит" SOC ниже лимита.
2. **Лимит заряда**: Нужно также ограничить заряд сверху, если установлен ручной режим `buy` с лимитом менее 100%.
3. **Безопасность**: Ручной лимит разряда не должен быть ниже `b_min_soc`. Нужно использовать `max(b_min_soc, manual_soc)`.

### Заключение
1. Модифицировать `run_soc_simulation` в `strategy_base.py`: извлекать `soc_limit` и устанавливать `h_floor_trade` / `h_ceiling_trade`.
2. Применить `h_floor_trade` к обеим фазам разряда (дом и продажа) для ручных режимов.
3. Версия -> `v11.9.601`.

## [2026-05-13 23:50] Задача: поднять ключевые атрибуты Inverter Mode Command в самый верх.

### Archi
Пользователь хочет видеть главные данные (`Strategy decision`, `Arbitrage decision`, `Target SOC`) сразу при открытии карточки сенсора, без прокрутки. В Home Assistant порядок в UI зависит от порядка ключей в словаре `extra_state_attributes`. Нужно пересобрать этот словарь так, чтобы важные поля шли первыми.

### Skeptic
1. **Хрупкость порядка**: Если атрибуты добавляются динамически (например, через `.update()`), порядок может нарушиться. Нужно использовать явное объявление словаря с нужной последовательностью.
2. **Второстепенные поля**: Поля типа `is_manual`, `icon`, `friendly_name` должны быть в самом конце.
3. **Согласованность**: Убедиться, что мы не потеряли ни одного атрибута при переезде.

### Заключение
1. В `sensor.py` переупорядочиваем атрибуты `InverterModeSensor`.
2. Порядок: `Strategy decision`, `Arbitrage decision`, `Buy decision`, `Sell decision`, `Target SOC`, `Power`, `Amps`, и далее остальные.
3. Версия -> `v11.9.585`.

## [2026-05-14 14:05] Задача: давай ка для ручного режима оставьм ограничени только мощность батареии и 15% soc минимальный, без всяких гатекиперов

### Archi
Пользователь просит максимально упростить ручной режим: убрать "умные" расчеты мощности (когда система снижала мощность, чтобы плавно дойти до лимита к концу часа) и отключить системные защиты (гатекиперы), оставив только физический предел мощности АКБ (`max_batt_p`) и жесткий минимальный порог в 15% SOC.
Для этого нужно:
1. В `strategy_base.py` установить жесткий пол `max(15.0, soc_limit)` для ручного режима разряда.
2. В `sensor.py` полностью выпилить логику `Anchor Power` (`req_p`, `p_calc`). Если заряд выше/ниже цели — просто выдаем `max_batt_p` мощности.

### Skeptic
1. **Отсутствие лимита в UI**: Если пользователь включит ручной режим, но не задаст ползунком `soc_limit`, система должна по умолчанию использовать `15.0%`, а не 0%.
2. **Слишком резкая остановка**: Если мы даем `max_batt_p`, то инвертор будет разряжать батарею на полную мощность, а по достижении 15% (или заданного лимита) резко обрубит мощность в 0. Но в рамках задачи это ожидаемое поведение ("оставь только мощность батареи").
3. **Перезаряд (Buy)**: Для ручного заряда (`buy`) тоже нужно убрать якорь и просто заряжать на `max_batt_p` до заданного лимита (или 100%).

### Заключение
1. В `sensor.py` (строки ~3166-3240) заменить блок `_manual_anchor` и `p_calc` на прямое присвоение `max_batt_p`.
2. В `strategy_base.py` обновить логику `run_soc_simulation`, чтобы пол всегда был `max(15.0, _m_soc_f)`.
3. Версия -> `v11.9.618`.

## [2026-05-14 16:21] Задача: с карточкой проблема. Работает только в локальной сети. Если снаружи смотрб выдает ошибку конфигурации

### Archi
Ошибка "Custom element doesn't exist" (ошибка конфигурации карточки) при доступе снаружи почти всегда связана с тем, что внешний доступ (через Reverse Proxy, NGINX, Cloudflare) или мобильное приложение (WebView) блокирует загрузку JS файла карточки.
Причины в текущем коде `__init__.py`:
1. Карточка раздается по пути `/api/energy_management/static/...`. Многие прокси-серверы из соображений безопасности по умолчанию блокируют внешние неавторизованные запросы к любым эндпоинтам, начинающимся с `/api/`.
2. В `CardStaticView` не включен CORS (`cors_allowed = True`), из-за чего WebView мобильного приложения может отклонить загрузку скрипта.
Решение: Уйти от префикса `/api/`, переименовав путь в `/energy_management_static/`, и включить CORS.

### Skeptic
1. **Обновление Lovelace**: Если мы изменим URL, обновится ли он в ресурсах Lovelace у пользователя? Да, наш код в `_async_register_lovelace_resource` ищет совпадение по имени файла и сам перезапишет URL на новый.
2. **Кэш**: Пользователю обязательно придется сбросить кэш мобильного приложения (или потянуть вниз для обновления) после применения патча, иначе приложение будет искать карточку по старому локальному пути.
3. **Безопасность**: Убрав `/api/`, мы просто выносим статику в корень (как делает HACS с `/hacsfiles/`). Это безопасно, так как мы отдаем только один конкретный JS файл.

### Заключение
1. В `__init__.py` заменить префикс URL карточки с `/api/energy_management/static` на `/energy_management_static`.
3. Сделать бамп версии до `v11.9.619`.

## [2026-05-14 16:25] Опровержение: "так тоже по api зарегена другая интеграция и работает"

### Archi
Пользователь прав. Если другая интеграция работает через `/api/...`, значит проблема не в префиксе URL, а в **обработчике** этого URL. 
В нашем коде используется самописный класс `CardStaticView`, который:
1. Не поддерживает CORS (`cors_allowed = True` отсутствует).
2. Не умеет обрабатывать запросы `HEAD` и `OPTIONS`, которые отправляют внешние прокси (Cloudflare, NGINX) для проверки доступности и кэширования.
3. Не поддерживает сжатие (gzip/brotli), ожидаемое внешним доступом по домену.

Решение: Полностью удалить самописный `CardStaticView` и использовать встроенный в Home Assistant метод `hass.http.register_static_path`. Он "из коробки" поддерживает CORS, сжатие и все необходимые HTTP-заголовки для внешних доменов.

### Skeptic
1. **Совместимость**: `register_static_path` — это стандартный API Home Assistant, он безопаснее и надежнее самописного View.
2. **Кэш**: Использование `cache_headers=False` при регистрации пути оставит кэширование на усмотрение браузера, но наш параметр `?v=v11.9.619` в URL всё равно принудительно обновит кэш у пользователя.

### Заключение
1. В `__init__.py` удалить класс `CardStaticView`.
2. В `_async_register_card` заменить `hass.http.register_view(...)` на `hass.http.register_static_path(...)`.
3. Оставить URL прежним (`/api/energy_management/static/...`).
4. Версия -> `v11.9.619`.

## [2026-05-14 16:31] Задача: Вернуть плавное регулирование мощности в ручном режиме

### Archi
Откатываем удаление Anchor Power из `sensor.py`. Мы неправильно поняли запрос "без гатекиперов и только мощность батареи" — пользователь имел в виду отключить системные защиты разряда (поставив дно 15%), а не отключать умное распределение мощности по времени.
Возвращаем формулы расчета:
- Для `buy`: `p_calc = (delta_kwh / hours_left) / eff`
- Для `sale_pv_bat`: `req_p = (delta_kwh / hours_left) * eff`
При этом сохраняем жесткий пол в 15% для ручного разряда.

### Skeptic
1. **Перезапись файлов**: Нужно аккуратно восстановить переменные `_manual_anchor_power` и логику `is_new_anchor`, иначе инвертор будет постоянно пересчитывать мощность каждую минуту и дергать реле.
2. **Лимит 15%**: Убедимся, что `t_soc = max(15.0, t_soc)` применяется ДО расчета дельты, чтобы система не пыталась разрядить ниже 15%.
3. **Согласованность**: Версия будет бампнута до `v11.9.620`.

### Заключение
1. В `sensor.py` восстановить блоки `Anchor Power` для ручных режимов `buy` и `sale_pv_bat`.
2. Бампнуть `const.py` и `manifest.json` до `v11.9.620`.
